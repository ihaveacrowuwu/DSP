[pytest]
# `slow` marks the tests that actually train something. They run by default — a
# reproducibility suite nobody runs proves nothing — but can be skipped with
# `-m "not slow"` when iterating on the fast contract and metric tests.
markers =
    slow: trains a model; a few seconds each on CPU
