The app's tint. System controls inherit it, which is the whole point of setting it
here rather than colouring controls one at a time.

This is the ONE colour shared with the dashboard's chrome. Everything else on this
screen comes from the platform's semantic palette.
