Mapped onto the platform's semantic role wherever one exists (systemRed for
destructive) rather than hardcoded. Defined here for the cases where no system role
carries the right meaning.

Destructive actions and failures only.
