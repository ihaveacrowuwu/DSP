<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Only the launch theme lives in XML. Everything the app draws afterwards is
         themed by MurakaTheme in Compose, so there is one source of colour, not two. -->
    <style name="Theme.Muraka.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Muraka</item>
    </style>

    <!-- The LIGHT parent. values-night/themes.xml overrides it with the dark one.
         `android:Theme.Material.DayNight` does not exist — the DayNight variants are an
         AppCompat thing, and this app has no AppCompat dependency because its interface is
         Compose. Two qualified resources do the same job with nothing added. -->
    <style name="Theme.Muraka" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/splash_background</item>
    </style>
</resources>
