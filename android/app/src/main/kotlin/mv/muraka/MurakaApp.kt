package mv.muraka

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable

/** Root composable. Replaced by the navigation host once the screens exist. */
@Composable
fun MurakaApp() {
    Text("Muraka")
}
